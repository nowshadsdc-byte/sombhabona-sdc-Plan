services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sombhabona-app
    restart: unless-stopped
    ports:
      - "8071:3000"
    environment:
      DATABASE_URL: "mysql://nowshad:ABCDefg12***@172.17.0.1:3306/SombhabonaSDC"
    extra_hosts:
      - "host.docker.internal:host-gateway"
